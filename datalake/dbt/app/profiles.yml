MacroLakehouse:
  outputs:

    dev:
      type: trino
      method: none  # optional, one of {none | ldap | kerberos}
      user: trino
      password: ''  # required if method is ldap or kerberos
      database: iceberg
      host: trino
      port: 8080
      schema: silver
      threads: 4

    prod:
      type: trino
      method: none  # optional, one of {none | ldap | kerberos}
      user: trino
      password: ''  # required if method is ldap or kerberos
      database: iceberg
      host: trino
      port: 8080
      schema: silver
      threads: 4

  target: dev
